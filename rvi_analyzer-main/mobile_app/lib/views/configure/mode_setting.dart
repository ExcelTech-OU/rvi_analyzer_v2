import 'package:flutter/material.dart';

class ModeSettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mode Settings'),
      ),
      body: Center(
        child: Text('This is the Mode Settings Page'),
      ),
    );
  }
}
